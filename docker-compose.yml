services:

  mysql:
    image: mysql:8.0
    container_name: mysql-db

    environment:
      MYSQL_DATABASE: producto_gyl
      MYSQL_ROOT_PASSWORD: rootsecret

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

    restart: unless-stopped

  adminer:
    image: adminer
    container_name: adminer

    ports:
      - "8081:8080"

    depends_on:
      mysql:
        condition: service_healthy

  backend:
    build: .

    container_name: spring-backend

    depends_on:
      mysql:
        condition: service_healthy

    ports:
      - "8080:8080"

    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/producto_gyl?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: rootsecret
      BACK_PORT: 8080

    restart: unless-stopped

volumes:
  mysql_data:
